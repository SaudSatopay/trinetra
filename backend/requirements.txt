# Trinetra backend dependencies.
# NOTE: WP1 (simulator) + WP2 (compound engine + benchmark) need NONE of these —
# they are pure standard library, so the core runs anywhere with Python 3.10+.

# --- API + realtime (WP3) ---
fastapi>=0.115
uvicorn[standard]>=0.32
websockets>=13
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.27

# --- Multi-agent orchestration (WP4) ---
langgraph>=0.2
# Gemini (text + embeddings) is called via the REST API with httpx — no heavy SDK.

# --- Computer vision over CCTV (WP4) ---
ultralytics>=8.3
opencv-python-headless>=4.10
numpy>=1.26

# --- Knowledge graph (WP5) ---
networkx>=3.0
# neo4j>=5.25  # optional — we use in-process networkx instead of a Neo4j server
